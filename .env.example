# Canonical site origin. Required (HTTPS) when IPB_DEPLOY_ENV=production.
SITE_URL=http://localhost:3000
# Set to "production" only on the real production deployment; anything else is served with noindex.
IPB_DEPLOY_ENV=development
# Development only: show draft entries locally. Ignored by production builds.
# IPB_PREVIEW_DRAFTS=1
