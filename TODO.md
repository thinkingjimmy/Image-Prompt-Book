# Image Prompt Book — 开发任务清单

需求基线：[PRD.md](./PRD.md)；首个案例：[完整内容与参数附录](./docs/examples/grokbot-capsule-icon.md)。

> 所有者设计调整（2026-09-23，覆盖 PRD 对应条款，已实现并测试）：详情改为 ImageFX 式极简布局——Prompt 只以站点语言显示与复制（取消输出语言选择、完整输出与韩文原文视图）；移除来源与许可面板，仅保留图片来源链接与一行 CC BY-NC 署名（许可要求）；移除案例 provenance/recipe 绑定文案、“非实时预览”角标与卡片“草稿预览”标签；首个案例改用来源站短版作为 v2.0.0 主模板并扩展为 7 个参数；新增 Use in ChatGPT；弹窗使用自绘滚动条；5 张来源站短版案例图已导入，但展示权利为 pending，发布前需取得许可。

状态（2026-09-23）：M0–M4 与 M5 中可自动化的测试项已实现并通过验收，记录见 [docs/testing/2026-09-23-verification.md](./docs/testing/2026-09-23-verification.md)。未勾选项需要维护者人工审核（许可、真实图片、发布）、真实部署或 GitHub 上运行，未因代码完成而勾选。只有实现与对应验收均通过，才将 `[ ]` 改为 `[x]`。

推荐顺序：M0 → M1/M2 → M3 → M4 → M5。真实图片与用途审核可以和开发并行，但公开上线前必须完成。任务 ID 不因调整顺序而重排。

## M0. 初始化、内容结构与首个案例导入

目标：项目可启动，内容有明确契约，开发者不需要重新编写首个案例的 Prompt。

- [x] IPB-001 初始化 Next.js App Router + TypeScript strict，固定兼容的稳定依赖、pnpm、Node 版本与 lockfile，禁止使用 canary 作为基线。
- [x] IPB-002 配置 Tailwind 与 shadcn/ui，记录所选 preset；建立基础排版、暖白主题、圆角、间距、焦点与控件样式。
- [x] IPB-003 初始化 next-intl、`en`/`zh-CN` 消息和显式语言路由；`/` 固定进入 `/en`，未知语言为 404。
- [x] IPB-004 配置 lint、typecheck、Vitest、Playwright 和统一脚本；README 中写出安装、开发、构建与测试命令。
- [x] IPB-005 建立 `content/prompts/{slug}` 与 taxonomy；实现 Zod 基础 schema，覆盖身份、版本、状态、日期、来源、参数、语言、许可、图片和生成记录。
- [x] IPB-006 定义唯一的 published 过滤规则，供列表、搜索、详情、分类计数和 sitemap 共用；草稿与 test fixture 不得公开。
- [x] IPB-007 将案例附录第 3–4 节逐项导入 meta/en/zh-CN 文件，初始保留 draft、pending 审核、空实测模型，不虚构发布记录。
- [x] IPB-008 将附录第 5 节完整韩文导入 `original.ko.txt`，UTF-8/LF、末尾一个换行；建立原文字节快照或 SHA-256。
- [x] IPB-009 将附录第 6–7 节完整中英文模板导入各自文件，不自行删减、不替换成短版。
- [x] IPB-010 将附录第 8 节完整五参数 JSON 导入，保留稳定 ID、所有标签、全部段落和默认值。
- [x] IPB-011 建立初始 `examples.json=[]`、ATTRIBUTION、来源许可与改动记录；明确 draft 可以无图，published 不可以。
- [x] IPB-012 实现跨文件验证：唯一 slug、分类合法、必填翻译、文件存在、真实图片 dimensions、来源 URL、参数 token 和默认值。
- [x] IPB-013 建立生产与测试内容目录隔离；测试用多卡片数据标为 fixture，不进入生产构建目录。
- [x] IPB-014 完成代码 MIT 的许可证文件与适用范围说明，显式排除第三方 Prompt、翻译/改编和独立图片权利；不要把整个内容库标成无条件商用。

阶段验收：无数据库或模型 Key 可启动；首个案例文本已准确导入；内容校验能拒绝错误记录。对应 AC-09/10/11/20/22。

## M1. Gallery、搜索与分类

目标：首页直接浏览视觉案例，列表状态可分享、可刷新、可分页。

- [x] IPB-015 实现紧凑导航：品牌、搜索、分类、排序、语言切换、GitHub、提交入口；不添加整屏营销 Hero。（2026-09-23 按所有者要求参考 jevable.com 调整：顶部仅图标 + 合一筛选胶囊 + 👋/提交，语言下拉与 GitHub 移至页脚，简介卡片改为三行）
- [x] IPB-016 实现 1/2/3/4/5 列响应式瀑布流，采用明确的列内阅读顺序与 DOM 顺序；预留图片空间。
- [x] IPB-017 实现卡片主图、标题、摘要、标签、作者、来源和多图数量；一个 Prompt 只占一张卡片。
- [x] IPB-018 使用真实详情 anchor，支持新标签和中键；来源/作者链接独立，避免嵌套交互元素。
- [x] IPB-019 实现有内容的分类路由、已声明标签、参考图需求提示；不显示无根据的已验证模型标志。
- [x] IPB-020 实现标题/摘要/分类/标签/关键词搜索、NFKC、英文大小写折叠、中文子串与多词 AND 匹配。
- [x] IPB-021 实现搜索、分类和多标签 AND 组合；搜索最长 100 字符、最多 5 标签、300ms debounce 和 Enter 立即提交。
- [x] IPB-022 实现 featured/latest 确定性排序与 slug 稳定次序；不伪造热门数据。
- [x] IPB-023 实现每页 24 条显式分页、实际链接、页码规范化、超范围 404 与搜索改变重置页码。
- [x] IPB-024 同步白名单查询参数和界面状态；输入 replace，分类/分页 push；刷新可恢复列表条件。
- [x] IPB-025 实现加载、搜索空态、目录为空、请求失败和图片加载失败；错误不得伪装成零结果。（请求失败由 `(site)/error.tsx` 提供重试；尚无强制服务端失败的自动化测试）
- [x] IPB-026 使用独立测试 fixture 验证多列、多页与长标题，不把 fixture 发布为真实内容。

阶段验收：小屏与桌面均可浏览；排序/筛选和网址一致；无图占位、作者来源或模型信息造假。对应 AC-01/02/23。

## M2. 详情、弹窗与案例图片

目标：从列表进入连续浏览体验，同时提供可独立访问的详情。

- [x] IPB-027 实现共用 `PromptDetail`，包括标题、用法、输入要求、版本、来源和许可。
- [x] IPB-028 实现 `/{locale}/prompts/{slug}` 独立详情；不存在、草稿与归档返回真实 404。
- [x] IPB-029 实现拦截路由 + Parallel Routes 弹窗，补齐 default/null/catch-all slot，避免离开后残留 modal。
- [x] IPB-030 实现桌面双栏与移动全屏，图片在上、编辑在下；处理 `dvh`、safe area 和底部操作区。
- [x] IPB-031 保留从列表进入时的筛选、页码、滚动和触发元素；关闭、后退、前进正确恢复。
- [x] IPB-032 区分拦截打开与直接详情；只有前者通过 back 关闭，后者提供明确的返回 Gallery 链接。
- [x] IPB-033 实现主图、缩略图、contain 显示、放大和失败回退；没有输入图不创造 Before/After。
- [x] IPB-034 显示 source-reported/project-verified 与 recipe 绑定状态；参数变化不重新生成、镜像或伪装更新图片。（按所有者要求简化：不再显示绑定文案；schema 与发布校验仍保留 provenance/recipe）
- [x] IPB-035 常驻“案例效果，非实时预览”，未知绑定显示未验证；只对完整且匹配的记录显示一致标记。（按所有者要求移除该角标；参数变化仍不改图）
- [x] IPB-036 实现 Customize/Output/Original 三视图；原文明确标为韩文，完整文本不被短摘要替代。（按所有者要求改为单一 Prompt 视图，只显示站点语言；韩文原文保留在内容与附录中）
- [x] IPB-037 实现作者、多个来源、许可与改动说明，以及指向实际内容文件的 GitHub 改进入口。（按所有者要求改为一行署名 + 图片来源链接 + 复制来源说明）
- [x] IPB-038 完成 Dialog 焦点约束、返回焦点、滚动锁和嵌套层 Esc；Select、大图优先关闭，不误关外层详情。

阶段验收：直接访问/刷新可读；普通点击弹窗；来源可打开；键盘和移动端不被困住。对应 AC-03/04/05/16/17/20。

## M3. 参数化 Prompt、完整双语与分享

目标：完整实现本产品的差异化功能，不停留在静态 Prompt 页面。

- [x] IPB-039 实现纯函数 `composePrompt` 与文本/token AST，仅支持白名单参数，不执行 eval、HTML、嵌套模板或外部 include。
- [x] IPB-040 根据稳定参数 ID 和选项 ID 管理唯一 selections 状态，不从 DOM 文本反向拼接 Prompt。
- [x] IPB-041 实现短值的句内 Select，以及构图/腮红/上色的短标签 + 完整段落展示。
- [x] IPB-042 接入附录中全部五项参数与所有选项；只开放明确列出的字段，不添加违反风格核心的选项。
- [x] IPB-043 修改选项时同步定制视图、完整输出和待复制字符串；整段替换不得残留相反规则。
- [x] IPB-044 实现 uiLocale/outputLocale/originalLocale 分离；明确显示当前编辑与复制语言。（按所有者要求：输出语言 = 站点语言）
- [x] IPB-045 切换 UI 语言保留 slug、选项 ID 和已选择输出语言；必要时转为新语言独立详情，不显示旧语言背景残留。
- [x] IPB-046 检查标题、摘要、选项、按钮、用法、alt、metadata、错误提示完整双语；韩文仅作为原文入口。
- [x] IPB-047 实现复制：Customize/Output 复制当前输出语言全文，Original 复制韩文原文并切换按钮标签。
- [x] IPB-048 实现复制失败时可全选/手动复制的回退；成功与失败反馈真实，不截断长文本。
- [x] IPB-049 实现恢复默认，只重置本案例参数，不改变界面语言、输出语言或原文。
- [x] IPB-050 实现独立“复制来源说明”，使用附录的现成中英文署名与许可文案。
- [x] IPB-051 实现带 slug/templateVersion 的 sessionStorage 草稿；存储不可用时功能仍可正常运行。
- [x] IPB-052 实现干净模板分享链接与 hash 设置分享；不分享全文、原列表搜索词或私人数据。
- [x] IPB-053 校验分享 hash 的长度、键、版本、枚举值；旧版本/损坏值提示并回退；优先级为 hash → 当前版本草稿 → 默认值。
- [x] IPB-054 完成 96 组合 × 2 输出语言的确定性测试，原文不可变测试与两个默认全文 golden snapshot。
- [x] IPB-055 完成四种构图联动、无腮红、平涂、特征数量与多语言切换回归；验证复制文本与输出预览完全一致。

阶段验收：五参数、完整输出、双语和复制均可用；192 次文本组合测试通过，但不把文本测试当成生图质量验证。对应 AC-06/07/08/09/10/11/12/13/14/15。

## M4. SEO、贡献流程与发布校验

目标：每条内容可被独立搜索和分享，贡献者能添加内容而不修改 React 业务代码。

- [x] IPB-056 服务端输出标题、摘要、默认完整 Prompt、用法、图像信息、来源与许可；无 JS 仍可阅读默认内容。
- [x] IPB-057 为首页、分类、详情与说明页生成各语言 title/description；详情使用具体效果名称而非关键词堆叠。
- [x] IPB-058 根据 PRD 索引矩阵实现 canonical：语言 self-canonical、无 hash、默认分页不统一指向第一页。
- [x] IPB-059 实现 en/zh-CN 互相 hreflang 与实际存在的 x-default；不生成缺失语言的虚假 alternate。
- [x] IPB-060 实现查询/标签/非默认排序 noindex,follow，不在 robots 中阻挡爬虫读取这些指令。
- [x] IPB-061 从统一发布目录生成 sitemap，使用真实修改日期，排除 draft/fixture/查询页和不存在的翻译。
- [x] IPB-062 配置 robots、预览环境 noindex、正确生产 SITE_URL 和 HTTPS URL 规范化。
- [x] IPB-063 添加真实图片 alt/caption、合理 OG 图片与事实一致的结构化数据；不使用未授权图或伪造评分。
- [x] IPB-064 实现 About、Contribute、Licenses 页面及双语说明，明确代码开源与内容授权不是一回事。
- [x] IPB-065 配置来源线索 Issue、完整模板 PR、翻译贡献和权利问题反馈模板；不要求公开提交敏感证明。
- [x] IPB-066 编写 CONTRIBUTING、内容目录/参数说明、许可证范围和安全报告流程。
- [ ] IPB-067 配置 CI：lint、类型、内容校验、单元/E2E、构建；外部 PR 不获得生产或写入密钥。（进度：`.github/workflows/ci.yml` 已编写，只读权限、`pull_request` 触发；各步骤已在本地执行通过，尚未在 GitHub 上运行）
- [x] IPB-068 实现 published 资源与授权审核 gate；自动检查只能验证记录完整性，人工审核仍需真实依据。
- [x] IPB-069 提供独立外链检查方式，原站失联不阻断构建、不删除来源、不自动替换作者链接。

阶段验收：HTML、metadata、canonical、hreflang、sitemap 均符合 PRD；内容贡献与许可可追溯。对应 AC-18/19/20/21/22。

## M5. 真实素材、综合测试与公开上线

目标：交付可运行、可维护的首发版本，不以假图、假授权或文档代替实现。

### 内容与人工审核

- [ ] IPB-070 由维护者确认首个 Prompt 在网站实际用途下的许可依据；涉及超出 CC BY-NC 范围的用途时，记录额外许可或改用适用内容。
- [ ] IPB-071 对至少一张真实案例图取得并记录展示依据；可使用获得许可的原站图，或维护者实际生成并具备所需权利的新图。（进度：已按所有者要求从来源站导入 5 张短版案例图，rights 记录为 pending；仍需取得并记录展示许可）
- [ ] IPB-072 处理真实图片为本地响应式素材，记录真实宽高、双语 alt、来源与审核证据，不将候选 URL 自动当成已获授权资源。（进度：已本地化为 1254×1254 JPEG，双语 alt 与来源已记录；待授权审核）
- [ ] IPB-073 核对结果 provenance/recipe；原站案例未知参数保持 null，自行验证记录才填写模型、版本、选项、日期。
- [ ] IPB-074 维护者复核中英文全文、五参数所有选项与原文署名，记录审查结果；不得声称原作者验证了本站新增变体。
- [ ] IPB-075 在文本、图片与用途审核通过后将首个条目改为 published，填写真实 publishedAt；确认只有一个条目而非多张图重复成多个 Prompt。

### 自动与人工测试

- [x] IPB-076 E2E 覆盖首页/分类进入、后退/前进、关闭、刷新、直接访问、新标签和切换语言。
- [x] IPB-077 E2E 覆盖搜索/标签/排序/分页 URL 恢复、无结果和不存在条目 404。
- [x] IPB-078 E2E 覆盖参数编辑、各语言复制、原文复制、重置、草稿、分享恢复与无效版本回退。
- [x] IPB-079 E2E 覆盖剪贴板拒绝、存储不可用、图片失败、来源失联、坏 token 和缺失翻译。
- [x] IPB-080 检查 375px 手机、平板、常见桌面宽度、Safari/Chromium/Firefox，保证无水平溢出与被遮挡的操作。（Playwright Chromium/Firefox/WebKit 引擎与 Pixel 7 仿真、375/600/768/800/1200/1500 宽度；非实体 Safari/手机）
- [x] IPB-081 完成键盘、可见焦点、读屏名称、Select/Dialog Esc 层级与 reduced-motion 检查。（axe WCAG 2.2 A/AA、焦点约束与归还、Esc 分层、reduced-motion；未使用真实读屏软件）
- [x] IPB-082 执行恶意 HTML/URL、未知 hash 参数、过长查询与 draft/fixture 访问测试。
- [x] IPB-083 检查服务端 HTML、真实图片加载、metadata、canonical、hreflang、分页和 sitemap；记录结果。（在 fixture 生产构建上验证；正式域名上线后需复查）
- [ ] IPB-084 在明确设备/网络条件下测量 LCP、CLS 与页面资源，处理大图与重复预取；不虚报未采样的现场 INP。（进度：`pnpm vitals` 已记录 fixture 基线，见 docs/testing；需真实案例图与生产环境复测）

### 发布与开源

- [ ] IPB-085 从全新克隆按 README 安装、测试、构建和启动，确认不需要数据库、模型 Key 或私有运行凭证。（进度：工作树副本 frozen install + `pnpm verify` 已通过；提交后需从 git 全新克隆复验）
- [ ] IPB-086 核对 SITE_URL、实际域名所有权与 DNS、HTTPS、部署预览 noindex、生产环境变量和日志，不提交凭证。
- [ ] IPB-087 仓库所有者确认公开时机、MIT 文件及第三方内容例外，然后执行或明确授权仓库公开；不把权限修改隐藏在脚本里。
- [ ] IPB-088 部署首版，确认至少一条合规真实案例、来源链接、完整双语、复制、分享和移动端行为。
- [ ] IPB-089 建立发布记录、回滚步骤、内容维护说明；公开“已实现/未实现”状态，不把未来路线图写成现有能力。
- [ ] IPB-090 由项目所有者按 PRD 的 AC-01–AC-24 最终验收，并将测试记录与已完成任务关联。

阶段验收：AC-01–AC-24 全部通过；代码、内容、授权和部署状态均有依据。

## 首版之后再评估

以下不属于上述里程碑完成的必要条件，不应提前挤占核心功能开发时间。

- [ ] P1-01 扩展日语、法语、西班牙语等完整内容包；不通过英文回退制造重复索引页。
- [ ] P1-02 增加自由文本/颜色等参数类型，重新评估隐私、草稿和分享机制。
- [ ] P1-03 暗色主题、本地收藏、用户偏好；先评估需求，不默认建设账号后端。
- [ ] P1-04 扩大经人工审核的原创或已授权案例库，而不是自动转载全网内容。
- [ ] P1-05 若加入站内生图、付费或广告，重新审核模型条款、内容许可、成本与数据隐私，另写需求。

## 完成记录要求

勾选任务时在相关 PR/commit 中注明任务 ID、对应 AC、测试命令与结果。人工授权、素材审核和上线检查应记录实际审核人、日期、依据；不得使用“TODO”“示例值”或编造链接填充通过状态。
