# Image Prompt Book — 网站需求与开发规格

- 版本：v1.0
- 编写日期：2026-09-23
- 状态：可进入开发的需求基线；不表示功能已实现或网站已上线
- 仓库：`thinkingjimmy/Image-Prompt-Book`
- 预期生产域名：`https://imagepromptbook.com`
- 任务清单：[TODO.md](./TODO.md)
- 规范性内容附录：[首个 Grokbot 案例](./docs/examples/grokbot-capsule-icon.md)

> 本文与案例附录共同构成完整需求。附录已经提供首个案例的完整韩文原文、中英文参数化全文、全部参数选项和使用文案。开发者负责导入、实现和测试，不需要重新编写 Prompt，也不能用省略版替换完整模板。

## 1. 产品定位与需求来源

### 1.1 一句话定位

一个开源的生图 Prompt Gallery：通过真实案例发现喜欢的视觉效果，用可理解的内联选项调整 Prompt，并以所选语言复制完整文本。

英文品牌统一使用 **Image Prompt Book**，不在不同语言下创建不同的英文品牌名。建议首页短文案：

- EN：**Explore image prompts. Make them yours.**
- 简中：**发现喜欢的效果，调整成自己的版本。**

### 1.2 已明确的用户要求

1. 项目开源。
2. 首页是图片优先的 Gallery 瀑布流，视觉参考 [Jevable](https://jevable.com/)，不是长篇营销落地页。
3. 点击卡片打开详情弹窗，展示案例图、Prompt 和原始来源；来源可能是网站、X、GitHub 等。
4. Prompt 中的关键信息以内联下拉选项呈现，修改选项后得到可复制的完整 Prompt。
5. 支持国际化，不能只翻译导航而不翻译模板和选项。
6. 重视 SEO，详情必须能被独立访问与理解。
7. 使用 [Grokbot Icon](https://grokbot-icon-studio.serio-ai.chatgpt.site/en) 作为首个案例，以用户本次提供的完整韩文 Prompt 为原文基线。

### 1.3 本稿采用的实施默认值

以下为缩小首版范围而做的产品与技术裁决，并非声称用户此前逐项指定过：

| 项目 | 首版默认值 |
| --- | --- |
| 网站界面与模板编辑语言 | `en`、`zh-CN`；默认入口为英文 |
| Prompt 输出语言 | 英文、简体中文；原始韩文单独只读、可复制 |
| 后续语言 | 日语、法语、西班牙语等通过内容包扩展，不属于首发交付承诺 |
| 技术栈 | Next.js App Router、TypeScript、Tailwind CSS、shadcn/ui、next-intl |
| 内容维护 | Git 文件、PR 审核、构建期校验；无管理后台 |
| 列表加载 | 每页 24 条、显式分页；首版不做无限滚动 |
| 账户与生图 | 不登录、不接支付、不调用站内生图 API |
| 代码许可 | 计划 MIT；第三方内容单独标注，不自动继承 |
| 视觉主题 | 浅色、暖白、轻边框、圆角；暗色模式后置 |

开始编写本文时，仓库为 private 且没有文件。此次文档提交不修改仓库可见性、DNS、部署配置或第三方授权状态。

## 2. 目标用户、价值与首版边界

### 2.1 核心使用流程

浏览图片 → 搜索或筛选 → 打开详情 → 查看案例与来源 → 调整参数 → 选择输出语言 → 复制完整 Prompt → 在外部生图工具中使用。

主要服务需要灵感与可复用模板的普通创作者、设计师、独立开发者，以及愿意整理和贡献优质 Prompt 的社区成员。

### 2.2 首版必须包含

Gallery、搜索、分类、标签筛选、精选/最新排序、分页、详情弹窗、独立详情页、图片切换与放大、内联参数编辑、完整输出与原文查看、复制/重置/分享、国际化、来源与许可、SEO、文件驱动内容、GitHub 贡献入口和自动化测试。

参数编辑和国际化是核心能力，不能在首版降级成只有图片与复制按钮的链接目录。

### 2.3 首版不包含

站内图片上传或生成、模型 API Key、实时效果预览、自动翻译 API、任意 URL 自动抓取并发布、用户账号、云收藏、评论、点赞、排行榜、支付、广告系统、后台 CMS、视频 Prompt 编辑器和任意全文富文本编辑。

新模板的参数由内容维护者显式定义，不通过运行时模型自动识别。首版参数引擎只需支持枚举型 `select`，不需要同时实现自由文本、滑块或颜色选择器。

### 2.4 内容数量与成功标准

技术验收可从一个真实案例开始，不为了凑瀑布流把同一 Prompt 拆成多个伪条目。开发测试可使用明确标为 fixture 的合成数据，绝不进入生产目录或 sitemap。

公开首发至少需要一个授权状态清楚、包含真实效果图、完整中英文模板及有效来源的已发布案例。后续内容目标可设为 12–24 个独立模板，但不是基础开发的前置条件。

产品首轮验收关注能否顺利完成“找到案例—修改—复制—查来源”，不预设搜索流量、排名或生成质量保证。

## 3. 信息架构与路由

所有用户可见内容路由使用显式语言前缀；slug 跨语言保持相同。

| 路由 | 用途 |
| --- | --- |
| `/` | 固定重定向至 `/en`，不根据机器人、IP 或浏览器语言返回不同主内容 |
| `/{locale}` | Gallery 首页 |
| `/{locale}/categories/{category}` | 分类 Gallery；首版启用有内容的分类 |
| `/{locale}/prompts/{slug}` | 可独立访问的详情页，同时是弹窗对应网址 |
| `/{locale}/about` | 产品说明、使用方法、开源与内容边界 |
| `/{locale}/contribute` | 提交来源、贡献模板与翻译的说明 |
| `/{locale}/licenses` | 网站代码与内容许可说明、权利问题反馈入口 |
| `/sitemap.xml`、`/robots.txt` | 搜索引擎入口 |

列表状态使用白名单查询参数：`q`、`tags`、`sort`、`page`。分类使用路由，不重复用 `category` 查询参数表达同一状态。

示例：`/zh-CN/categories/avatars?q=机器人&tags=minimal,2d&sort=latest&page=2`。

未知条目、草稿、停用分类、不支持的语言返回真实 404，不返回 200 空壳。合法的旧 slug 在存在迁移记录时才做永久重定向。无效页码统一规范为第一页；超过总页数且总数非零时返回 404；无结果的第一页展示空状态。

## 4. 首页与 Gallery 视觉规格

### 4.1 页面布局

参考用户截图及 [Jevable](https://jevable.com/) 的内容优先结构：顶部紧凑导航，下面直接展示图片。只借鉴布局和交互方向，不复制品牌资产、文案或第三方案例图。

- 左侧：Image Prompt Book 字标或简洁图标，返回当前语言首页。
- 中间：搜索框、分类、排序；窄屏可折为第二行。
- 右侧：语言切换、GitHub、提交 Prompt。
- 导航下方最多一行 H1 与短介绍，不设置整屏 Hero、视频背景或价格区。
- 图片区保持宽幅；页边距移动端 16px、桌面端 24–32px，卡片间距 16–24px。
- 建议暖白背景 `#F7F6F3`、近黑文字和浅灰边框；参数选项使用低饱和淡紫色强调。颜色为设计起点，需通过对比度验收。
- 卡片图片圆角约 16–20px，不使用厚重阴影或过多边框。

### 4.2 响应式列数

| 视口宽度 | 目标列数 |
| --- | --- |
| `< 480px` | 1 |
| `480–767px` | 2 |
| `768–1023px` | 3 |
| `1024–1439px` | 4 |
| `≥ 1440px` | 5 |

首版推荐成熟的 CSS 多栏布局配合 `break-inside: avoid`。排序阅读顺序明确为“每列自上而下，再到右侧下一列”，DOM 和键盘顺序与之保持一致；不承诺逐行左右排序。不要使用实验性 masonry 语法作为唯一实现，也不要用 absolute 定位打乱焦点顺序。

使用显式分页而非追加式无限滚动，避免多栏追加导致已有卡片反复重新分配。图片必须有真实宽高或 aspect-ratio，加载前预留空间。

### 4.3 卡片内容

主图 → 标题 → 最多两行简介 → 最多三个标签 → 作者/来源。

主图保留原比例，默认不裁成正方形；多案例时显示 `+N`，同一 Prompt 仍只占一张卡片。过长图片在导入时提供单独的 gallery 展示图，不在运行时任意截断重要信息。

标题与主图使用真实详情 `<a>` 链接，支持中键、Cmd/Ctrl 点击和新标签。作者和来源链接独立，不能嵌套在整卡按钮或另一个 anchor 内；点击来源不能顺带打开详情。

来源作者未知时显示“来源网站”，不得猜测作者身份。没有经过测试的模型不显示“支持全部模型”“已验证”等标志。首个案例必须显示“需要参考图”。

### 4.4 搜索、筛选、排序与分页

- 搜索范围：中英文标题、摘要、分类、标签及显式维护的关键词；首版不搜索整段原文。
- 输入 trim、Unicode NFKC 规范化和英文大小写折叠；采用词项子串匹配，多个搜索词为 AND。支持中文子串，不承诺分词或模糊语义搜索。
- 分类单选；多个标签为 AND；搜索、分类和标签之间也是 AND。
- 查询最长 100 字符、最多 5 个标签；未知标签忽略并规范 URL。
- 输入 300ms debounce，Enter 立即提交；搜索和筛选更改重置 `page=1`。输入使用 replace，避免每个字生成历史记录；分类与分页点击使用 push。
- `featured`：有精选排名的条目优先，之后按发布日期倒序；并列以 slug 稳定排序。不是只显示精选条目。
- `latest`：按 `publishedAt` 倒序，slug 作为稳定次序。
- 每页 24 条。分页使用可抓取的真实链接，显示当前页、上一页、下一页。不足一页时不显示无效分页。
- 空状态展示已应用筛选与“清除筛选”；失败状态提供重试，不用“零条内容”掩盖服务错误。

## 5. 详情弹窗与独立详情页

### 5.1 结构

桌面端使用双栏对话框，宽约 `min(1200px, 94vw)`、最大高度约 `90dvh`；左侧约 45% 展示图片，右侧约 55% 展示模板。窄屏使用全屏对话框，图片在上、编辑区在下，底部操作区预留安全区。

详情页和弹窗共用同一 `PromptDetail` 内容组件、数据模型与 metadata 生成逻辑，不复制两套业务。

### 5.2 图片区

包含主图、缩略图、查看大图、图片来源和复现状态。使用 `object-fit: contain` 展示完整图像，放大图层可关闭。

首版**不根据选项生成或修改图片**，参数改变也不自动替换成一张看似对应的图片。固定显示“案例效果，非实时预览”。

案例绑定分为：

- `source-reported`：原作者展示的结果，本站未复现；不知道精确模板版本、参数或模型时保持 unknown/null。
- `project-verified`：维护者实际生成并记录了模板版本、输出语言、完整参数、模型与日期。

只有完整绑定与当前选择一致时，才能显示“与当前参数一致”。未知绑定显示“原站示例，未验证与当前参数一致”；变更参数后不得继续显示一致标志。首版不提供“应用图片参数”按钮，以免引入不完整绑定。

### 5.3 右侧内容

标题、摘要、需要参考图的提示、模板版本、来源与许可摘要，随后是三个视图：

1. **定制 / Customize**：在完整模板中嵌入参数选项，按语义章节分段阅读。
2. **完整输出 / Output**：当前参数与输出语言下的完整纯文本，默认值在首屏 HTML 中可访问。
3. **原文 / Original**：未经参数化的韩文原始 Prompt，只读并明确语言。

长文本可放在有明确标题的可折叠段落中，不能只保留一句摘要。控制区与来源在手机端同样可访问。

### 5.4 路由与关闭行为

采用 Next.js [Intercepting Routes](https://nextjs.org/docs/app/api-reference/file-conventions/intercepting-routes) 与 [Parallel Routes](https://nextjs.org/docs/app/api-reference/file-conventions/parallel-routes) 所描述的路由弹窗模式。

| 行为 | 验收结果 |
| --- | --- |
| 从首页/分类页普通点击 | 打开弹窗，地址变为详情网址，底层列表保留 |
| 关闭或浏览器后退 | 返回此前筛选、页码及滚动位置 |
| 浏览器前进 | 能重新进入该详情 |
| 直接打开详情/刷新详情 | 展示完整独立详情，不依赖曾经访问首页 |
| 新标签打开 | 展示独立详情 |
| 切换网站语言 | 保留同一 slug 与参数草稿；允许以新语言独立详情重新呈现，不强求保留旧语言背景弹窗 |

只有明确记录“从本站列表拦截打开”的情况才使用 `router.back()` 关闭；不能仅根据 `history.length` 判断，否则可能把用户送回站外。独立详情显示“返回 Gallery”的正常链接。

### 5.5 可访问性

遵循 [WAI-ARIA 模态对话框模式](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/)：可读标题、焦点移入、焦点约束、关闭后返回触发点、背景不可交互。长内容初始聚焦标题或容器，不自动聚焦会触发手机键盘的控件。

Esc 只关闭最上层：先关闭 Select/Popover，再关闭大图，再关闭详情。外层不要重复接收内层已经处理的 Esc。关闭按钮始终可见；滚动锁定必须在关闭与卸载时恢复。

## 6. Prompt 参数编辑与复制契约

### 6.1 原文与可定制版本分离

原始 Prompt 是不可变来源记录。可定制版本是明确注明翻译与改编的派生内容，具有独立 `templateVersion`；修改参数不会修改原文，也不会触发外部请求。

不能把“可编辑版恢复默认”等同于“逐字恢复原始韩文”。默认可编辑版是中英文改编模板，原始韩文通过 Original 单独访问。

### 6.2 内联选项

短值直接在句子中呈现，例如“背景为 [近黑炭灰 ▾]”。每个选项有稳定 ID、当前界面语言的标签、各输出语言的实际替换值。

对于构图、腮红、上色等整段替换：在对应章节位置显示一个紧凑下拉标签，并在其下显示所选的完整段落。不能把几百字塞进下拉按钮，也不能只改章节标题而保留相互矛盾的旧段落。

除该案例明确开放的五个字段外，不暴露肤色、头发颜色、眼睛数目、嘴鼻或 3D 风格选项。原图身份特征来自用户之后提供给外部工具的参考图，不在本站假定或上传。

### 6.3 纯函数与数据流

```ts
composePrompt({ record, selections, outputLocale }): string
```

输入必须经过 schema 校验；输出是完整纯文本。维护唯一的 selections 状态，编辑视图、输出视图、复制和分享都读取它，禁止从 DOM `innerText` 拼出 Prompt。

模板语法仅接受 `{{parameterId}}`。将模板解析为文本与 token 的安全节点，再按白名单替换。替换值是最终叶子字符串，不允许嵌套模板、JavaScript、HTML、eval、任意表达式或远程 include。

一次替换所有同名 token；不依靠只替换第一个匹配的字符串操作。生产模板存在未声明 token、未使用字段、缺失语言值或替换值内含 token 时，内容校验失败。

统一换行为 LF；输出保留段落与章节、没有 Markdown fence、没有参数 ID 或界面按钮文字，结尾保留一个换行。

### 6.4 操作定义

| 操作 | 定义 |
| --- | --- |
| 在 Customize/Output 点击复制 | 复制当前 selections + outputLocale 的完整生成文本 |
| 在 Original 点击复制 | 按钮变为“复制韩文原文”，复制原文；输出语言选择器隐藏 |
| 复制来源说明 | 复制作者、来源链接、许可链接及修改说明；与用于生图的纯 Prompt 分开 |
| 恢复默认 | 只重置当前案例参数，不改变网站语言、输出语言或原文 |
| 分享模板 | 复制当前详情的干净网址，不带来源页面、搜索词或草稿 |
| 分享当前设置 | 在详情网址 hash 中记录版本、输出语言与枚举选项；不携带全文 |

复制成功后给出明确反馈；Clipboard API 失败时展示可全选文本与手动复制提示，不虚报成功。重置成功给出轻量提示，不增加全局撤销系统。

### 6.5 草稿与分享状态

同一标签页使用 sessionStorage 保存枚举参数与输出语言；key 包含 `schemaVersion + slug + templateVersion`。无账号同步，关闭标签页不承诺长期保存。存储被禁用时仍能完成编辑和复制。

状态优先级：合法的分享 hash → 当前版本 session 草稿 → 默认值。hash 存在但损坏或版本不兼容时，提示后使用默认值，不悄悄使用接收者的旧草稿。

规范 hash：`#v=1&template=1.0.0&output=en&p.composition=left-standard&p.background=charcoal&p.blush=subtle&p.shading=soft&p.featureBudget=three`。

只接受已知键和合法枚举，顺序固定、长度不超过 2048 字符。未知键忽略；无效已知值回退并提示。未来更改模板导致 ID 或语义不兼容时必须更新 templateVersion。

页面参数修改不应逐次改变浏览器历史；复制“分享当前设置”时才构建链接。详情 canonical 与 sitemap 都忽略 hash。

## 7. 国际化契约

### 7.1 三层语言

| 层次 | 管理范围 |
| --- | --- |
| `uiLocale` | 导航、按钮、错误提示、卡片文案、参数标签、定制视图完整文本 |
| `outputLocale` | 完整输出视图和复制的实际语言 |
| `originalLocale` | 原始来源文本语言；首个案例为 `ko` |

首次进入案例时 outputLocale 默认与 uiLocale 相同；之后由用户显式选择。切换界面语言保留已选 outputLocale，顶部始终清楚显示“编辑语言”和“复制输出语言”。使用稳定 option ID 保留选择，不通过翻译后的显示文字反查 ID。

首版英文与中文可编辑模板均已在案例附录提供。韩文原文不是韩文 UI 或韩文参数化功能，不把它列入网站语言切换器。

### 7.2 内容完整性

每个正式发布条目必须同时拥有中英文标题、摘要、使用说明、参数标签和完整模板；首版不允许靠英文回退页批量生成其他语言的可索引网址。缺失必填翻译的条目留在 draft。

网站通用界面消息使用 [next-intl](https://next-intl.dev/docs/routing/configuration)；Prompt 模板为独立内容资源。每种语言维护完整句式，不逐词拼接出句子，不请求运行时 AI 翻译。

`lang`、日期格式、按钮可读名称、alt、metadata 和错误状态都必须国际化。切换语言保留 slug，不产生新的内容 ID。

## 8. 内容模型与目录规范

### 8.1 目标目录

```text
content/
  taxonomy.json
  prompts/
    grokbot-capsule-icon/
      meta.json
      original.ko.txt
      en.json
      zh-CN.json
      template.en.txt
      template.zh-CN.txt
      parameters.json
      examples.json
      ATTRIBUTION.md
public/
  examples/
    grokbot-capsule-icon/
      [已获权利确认的真实案例文件]
src/
  app/[locale]/(site)/...
  components/gallery/
  components/prompt/
  lib/content/
  lib/prompt/
  lib/seo/
  i18n/
messages/
  en.json
  zh-CN.json
tests/
  fixtures/                 # 不从此目录生成生产内容
  unit/
  e2e/
```

`[已获权利确认的真实案例文件]` 为目录说明，不是允许在生产保留的假文件名。开发时按附录的素材清单导入，不凭空创建“已经生成”的案例。

### 8.2 必须表达的数据

| 对象 | 必填/语义 |
| --- | --- |
| 条目身份 | schemaVersion、id、slug、templateVersion；id/slug 唯一稳定 |
| 发布 | status：draft/published/archived；createdAt、updatedAt；published 才有 publishedAt |
| 分类 | 已声明的 category 与 tag ID；不能随意在展示层创造新标签 |
| 语言 | originalLocale、contentLocales、outputLocales、对应文件路径 |
| 来源 | sources[]：ID、website/x/github/other 类型、原始/补充角色、HTTPS URL、名称、可选作者、核对日期 |
| 参数 | ID、default、选项 ID；每种语言的 label、replacement |
| 许可 | Prompt 许可证及来源许可页、署名、修改说明、发布审核状态与证据 |
| 兼容性 | 需要参考图与否、来源推荐工具、本站实测记录；推荐与实测不得混淆 |
| 图片 | id、本地路径、真实宽高、各语言 alt、来源、权利依据、展示状态 |
| 结果绑定 | source-reported/project-verified；可空的模板版本、参数、输出语言、模型、生成日期 |

生产发布谓词统一封装：条目 `published`、本地必填资源完整、Prompt 使用范围审核通过、至少一个允许展示的真实案例、翻译与参数校验通过。首页、搜索、详情、分类计数和 sitemap 必须共用同一个过滤结果。

Draft 允许 `examples=[]` 并用于内容开发；Production 发布校验不能因此被放宽。测试 fixture 不进入公开 URL，也不能通过猜 slug 访问。

### 8.3 构建校验

使用 TypeScript + Zod 校验 JSON，再执行跨文件检查：文件存在、真实图片尺寸、唯一 ID、分类合法、来源 HTTPS、license/evidence 结构、完整语言包、token 对应关系、默认选项和所有替换值。

构建不得依赖来源网站在线：内容与图片由仓库提供，原站短时故障不应让本站构建失败。外链可由单独脚本人工或定期检查，失效时保留原始地址并显示状态；不能自动删除来源。

不执行贡献者提供的 MDX/JS。说明文案可以 Markdown，但禁用原始 HTML 并安全渲染；本例 Prompt 本身一律作为纯文本处理。

## 9. 首个案例：极简机器人头像

- ID/slug：`grokbot-capsule-icon`
- EN 标题：`Minimal Bot Icon — Grokbot Style`
- 简中标题：`极简机器人头像 · Grokbot 风格`
- 分类：`avatars`
- 标签：`minimal`、`2d`、`bot-icon`、`image-to-image`
- 输入：一张人物或角色参考图，由用户在外部生图工具中附上；本站不接收该图片。
- 原始语言：韩文。
- 可定制输出：英文、简体中文。
- 首版开放参数：构图、背景、腮红、上色、特征数量，共五项。

必须保持的风格核心：两个接近黑色的实心胶囊眼、无嘴鼻的圆润脸、两个眼睛完整可见、保留参考图主体的基本颜色与关键特征、极简 2D、单个主体、正方形画布。

不得因为名字里有 Grokbot 就把“由 Grok 生成”作为事实。来源网站推荐了 ChatGPT，但这只是来源说明，不是本站已验证模型。具体模型版本与生成参数未知时保持未知。

**完整原文、可定制全文、所有选项替换段落、现成中英文使用说明、图片候选与验收组合都见[规范性附录](./docs/examples/grokbot-capsule-icon.md)。不得将这项任务改写为“开发者自行整理 Prompt”。**

## 10. 来源、署名与许可

### 10.1 源信息展示

详情中常驻展示作者、原始来源、许可及修改标记。支持多个来源链接；GitHub 文件优先记录具体文件及已知提交链接，X 记录实际原帖 URL。只有主页时保留主页，不伪造帖子链接。

作者、整理者、翻译者、案例图提供者分别记录。明确改编不代表原作者为本项目背书。

### 10.2 首个案例的已核对限制

来源站[许可页](https://grokbot-icon-studio.serio-ai.chatgpt.site/en/license)将 Prompt 标注为 **CC BY-NC 4.0**，署名为 **APG / @multi_serio_ai**，并明确图片等素材不因一同展示就自动适用此许可。将该限制记录到条目数据与附录，而不是只放在站点 Footer。

[CC BY-NC 4.0](https://creativecommons.org/licenses/by-nc/4.0/)要求相应署名、许可链接、修改说明，并包含非商业条件。不要将“免费访问”“没有登录”自动当作已经满足非商业条件；公开部署前由维护者结合实际用途确认，超出授权范围时另行取得许可。此处是内容发布要求，不是对任何具体商业模式的法律结论。

案例 Prompt 的翻译和参数化改编保留其许可与改动说明，不重新标为 MIT。来源图尚未核对独立许可，开发阶段仅记录候选 URL；不默认盗链或打包。可以在确认输入与结果使用权的前提下，由维护者自行生成并提供真实新案例。

### 10.3 代码开源与公开上线

代码计划使用 [MIT](https://opensource.org/license/mit)，但 README、内容目录与 ATTRIBUTION 必须明确第三方内容例外。仓库可见性变更由仓库所有者在发布清单中执行或另行授权，不作为写文档或部署命令的隐含副作用。

内容上线前的两个显式门槛：确认此 Prompt 在本站实际用途下的许可依据；补齐至少一张真实、可展示的案例图及权利记录。两者不阻塞框架、编辑器与本地测试开发。

公开分享 Prompt 时应携带来源与许可说明。给模型输入的复制按钮输出纯 Prompt；另有“复制来源说明”供用户公开发布时使用。不要声称复制按钮提供任何额外商用权利。

## 11. SEO 与可索引性

### 11.1 渲染原则

使用服务端可渲染内容，不把所有详情藏在客户端状态里。直接请求详情 HTML 时，至少获得标题、摘要、案例图片信息、默认完整 Prompt、用法、来源与许可。默认输出可以在正常可展开的 details 内展示，不能只给爬虫显示与用户不同的内容。

卡片、分类和分页都是实际链接；没有 JavaScript 时仍能阅读默认详情与原文，但内联编辑和自动复制可依赖 JavaScript。

### 11.2 URL/索引矩阵

| 页面 | robots | canonical |
| --- | --- | --- |
| 首页、有效分类第一页 | index,follow | 当前语言无冗余参数的网址 |
| 默认排序且无搜索/标签的分页 N>1 | index,follow | 自身 `?page=N`，不能全部指向第一页 |
| 有 q/tags 或非默认 sort 的列表 | noindex,follow | 自身规范化查询网址；不进入 sitemap |
| 已发布详情 | index,follow | 当前语言详情，不含 hash |
| About/Contribute/Licenses | index,follow | 自身语言网址 |
| 不存在/草稿/归档条目 | 404 | 不生成有效内容 canonical |
| 部署预览/测试站 | noindex | 使用部署级限制，不混入生产 sitemap |

不要在 robots.txt 中屏蔽需要爬虫读取 `noindex` 的筛选页。分页规则参考 [Google 分页指导](https://developers.google.com/search/docs/specialty/ecommerce/pagination-and-incremental-page-loading)。

### 11.3 国际化 SEO

中英文内容各自 self-canonical，互相提供 `hreflang="en"` 与 `hreflang="zh-CN"`，`x-default` 指向对应英文页面。只为真实存在的翻译生成 alternate；不能把所有语言 canonical 到英文，也不能给未翻译的韩文 UI 生成 hreflang。参考 [Google 本地化页面指导](https://developers.google.com/search/docs/specialty/international/localized-versions)。

### 11.4 Metadata 与结构化数据

首页建议 title：`AI Image Prompts with Examples & Editable Templates | Image Prompt Book`。

详情 title 使用具体效果名称；description 写清用途、输入要求和可调整项，不堆模型关键词。每个案例具备真实、语义明确的 alt、宽高、caption 和邻近说明；参考 [Google 图片 SEO 指导](https://developers.google.com/search/docs/appearance/google-images)。

OG 使用已获展示权限的案例图；图片未获权利确认时不把它放进分享卡。部署预览不使用生产 canonical 冒充正式发布。

可输出与页面事实一致的 WebSite、CollectionPage、BreadcrumbList 及 [CreativeWork](https://schema.org/CreativeWork) 描述；不要伪造 AggregateRating、评论、下载量或效果评分，也不承诺富结果展示。

Sitemap 从已发布目录生成，包含实际语言版本及真实内容修改日期。不按构建时间把所有页面假装每天更新。保留原始来源，不能为避重复内容而删除署名。

## 12. 技术方案与职责边界

### 12.1 基础选择

Next.js App Router + TypeScript strict；初始化时使用相互兼容的稳定版本，不用 canary。将 Next/React、pnpm、Node 版本和 lockfile 固定在仓库中，不能长期依赖未锁定的 latest。

Tailwind 管理样式；shadcn/ui 用于 Dialog、Select、Tabs、Button 等。具体底层实现以初始化时选定的 shadcn preset 为准，不假定它必须是某一套组件底座。

next-intl 管理通用文案和路由语言；Zod 校验内容；Vitest 做纯函数/内容测试；Playwright 做关键用户流程测试。

默认 Vercel 部署，也允许标准 Node 运行。使用 `next build`/`next start`，不为了静态内容强制 `output: export`，避免与查询参数服务端过滤和路由策略发生冲突。

### 12.2 服务端与客户端

服务端加载受审核内容、生成目录、执行筛选和分页、输出 metadata 与默认详情。客户端只负责弹窗行为、选项状态、图片切换、草稿、复制和分享。不得把整站全部全文作为首页必需的客户端 bundle。

MVP 无数据库、无账号、无外部搜索服务、无生图 SDK、无后台写入 API。运行站点不需要模型密钥或 GitHub Token。

唯一必要的生产配置是有效 `SITE_URL`；开发环境使用 localhost。展示的 GitHub 链接使用配置中的仓库地址，不把私人凭证发送到浏览器。

### 12.3 路由布局建议

```text
app/[locale]/(site)/
  layout.tsx                      # children + modal
  page.tsx
  categories/[category]/page.tsx
  prompts/[slug]/page.tsx          # 独立详情
  @modal/
    default.tsx                   # null
    page.tsx                      # null
    (.)prompts/[slug]/page.tsx     # 被拦截的详情
    [...catchAll]/page.tsx         # null，离开详情时清除旧 modal
```

以上是目标结构，不是保证无需测试即可工作的代码。分类深层导航、locale 切换、关闭后再次前进、直接刷新必须由 E2E 验证。Parallel Route slot 的默认与空匹配分支不能省略。

### 12.4 图片与性能

使用 Next Image 或等效响应式图片方案，参考 [Next Image 文档](https://nextjs.org/docs/app/api-reference/components/image)。每张图提供真实 dimensions 与 sizes，Gallery 使用合理压缩的展示图，详情再加载较大图片。只优先加载预计的首屏主图，其他按需加载；不自动预取整站详情全文。

图片为审核后的本地资产或固定可信存储，不运行任意 URL 代理。现阶段不依赖外站图片热链。

目标而非实测结果：在约定的移动设备/网络测试条件下，LCP ≤2.5s、CLS ≤0.1；有足够真实访问样本后再评估 INP ≤200ms。每次记录 Lighthouse/浏览器测量条件，不把本地一次跑分写成全站保证。

## 13. 贡献流程与开源维护

“提交 Prompt”进入本站 Contribute 页面，提供“提交来源线索”和“提交完整模板”两个 GitHub Issue/PR 入口。第一种只要求来源、说明与可联系作者信息，不能要求贡献者必会写代码。

完整内容贡献流程：新增独立目录 → 填元信息/来源/许可 → 提交原文与中英文模板 → 声明参数和示例 → CI 校验 → 维护者审查 → 合并 → 发布。

CI 只证明结构与可测试规则，不证明内容授权或生图质量；人工审查结果必须有审核人、日期和依据。版权/隐私问题可通过指定 GitHub Issue 类型联系维护者；不要求公开敏感身份证明。

提供 CONTRIBUTING、内容提交模板、翻译说明、PR 模板和安全报告说明。详情的“在 GitHub 改进此模板”定位实际文件路径。

初版不自动爬取 X 或 GitHub，不要求提交者交出账号 Cookie，也不运行贡献者提供的脚本获取图片。

## 14. 安全、隐私与可靠性

Prompt 与来源文本均为内容数据，不是系统可执行指令。安全转义显示；URL 只允许合法 HTTPS 外链；新窗口链接使用合适的 rel，禁止 javascript/data 协议和 HTML 注入。

本站不上传参考图，不把用户参数传给模型服务，不记录用户输入的图片身份信息。share hash 仅含枚举 ID。analytics 首版不是运行依赖；若后续接入，默认不发送 Prompt 全文、搜索原文或个人素材。

GitHub PR 的内容校验运行在无生产密钥环境；不要用可获得写 Token 的 workflow 执行外部贡献者任意代码。依赖与运行环境按已锁版本检查。

异常处理至少覆盖：内容校验失败、原站失联、图片 404、复制权限拒绝、sessionStorage 不可用、分享参数无效、模板版本变化、翻译缺失、详情不存在和请求错误。

图片失败时保留布局、显示失败提示及来源入口，不自动替换为不相关案例。来源链接失效不应阻止复制本站已经合法保存的 Prompt。

## 15. 测试与验收矩阵

| ID | 场景 | 必须通过的结果 |
| --- | --- | --- |
| AC-01 | 打开英文/中文首页 | 对应语言、真实卡片、无整屏 Hero；单案例不会被复制成多条 |
| AC-02 | 搜索、标签、排序、分页 | 结果与 URL 一致；刷新可恢复；分页链接可直接访问 |
| AC-03 | 卡片点击/新标签 | 普通点击弹窗，新标签独立详情 |
| AC-04 | 后退、前进、关闭 | 列表筛选、页码、滚动恢复；不会跳回站外 |
| AC-05 | 直接访问/刷新详情 | 返回完整详情和正确 HTTP 状态，不出现缺失 slot |
| AC-06 | 五个参数逐一改变 | 选项、可读段落、完整输出、复制文本保持一致 |
| AC-07 | 构图四个变体 | 左右裁切、旋转方向、眼睛高低、留白四者一致 |
| AC-08 | 无腮红/完全平涂 | 不残留强制腮红、阴影或高光的矛盾指令 |
| AC-09 | 96 种组合 × 2 种输出语言 | 192 个确定性输出全部生成，无未替换 token，核心约束保留 |
| AC-10 | 默认输出 golden snapshot | 两种语言均匹配从附录导入的完整基线，不遗漏章节 |
| AC-11 | 韩文原文 | UTF-8/LF 原文保持不变；定制不影响原文复制 |
| AC-12 | UI 与输出语言分别切换 | 选择 ID 不丢失，当前输出语言明确，无静默翻译 |
| AC-13 | 分享当前设置 | 新标签能恢复参数；坏 hash/旧版本有明确回退提示 |
| AC-14 | 草稿存储失败 | 页面仍可编辑、复制和分享，无阻塞异常 |
| AC-15 | 复制成功/失败 | 成功复制完整文本；拒绝授权时提示手动复制 |
| AC-16 | 图片与参数不匹配 | 显示非实时与未验证说明，不假装生成新图 |
| AC-17 | 手机和键盘 | Dialog/Select/Esc/焦点恢复可用，底部按钮不遮内容 |
| AC-18 | SEO HTML | title、正文、默认完整 Prompt、来源和图片信息在 HTML 中可读 |
| AC-19 | canonical/hreflang/sitemap | 翻译互链，分页 self-canonical，筛选 noindex，draft 不公开 |
| AC-20 | 许可与来源 | 作者、出处、CC BY-NC、修改说明可见，未授权图不能 published |
| AC-21 | 恶意内容/URL | 被转义或拒绝，不执行脚本，不加载任意代理 URL |
| AC-22 | 从零克隆运行 | 固定依赖、无数据库和模型 Key，按 README 可启动并构建 |
| AC-23 | 空内容与坏资源 | 正确的空态/错误态，不伪造案例或视图计数 |
| AC-24 | 生产发布 | 指定真实域名、HTTPS、至少一个合规真实案例、无 fixture/draft 泄漏 |

## 16. 里程碑与完成定义

- M0：初始化、内容 schema、首个案例文本导入与许可证边界。
- M1：Gallery、搜索筛选和分页，真实链接与响应式布局。
- M2：独立详情、路由弹窗、图片浏览、来源与原文。
- M3：五参数编辑、完整输出、双语、草稿与分享。
- M4：SEO、贡献流程、来源与发布校验。
- M5：自动测试、真实素材、性能检查与生产发布。

具体 Markdown 任务统一维护在 [TODO.md](./TODO.md)，避免在多处重复维护完成状态。

某项功能只有在实现、测试通过且验收记录可查时才能勾选。文档包含代码片段、示例 URL 或完整 Prompt，不等于该功能已实现。发布素材与授权由维护者补齐，不能由开发者随意编造。

## 17. 交付物与上线检查

最终应交付可运行站点、锁定依赖、内容导入器、首个案例文件、自动测试、开源贡献说明、明确范围的许可证文件、可部署说明和测试记录。

公开上线前必须确认：实际域名配置、仓库公开决定、Prompt 使用依据、真实案例图片与权限、原作者署名、完整语言包、无密钥、无草稿/fixture 页面、正确 sitemap/robots/canonical、HTTPS 和失败回退。

首版不因“以后可能需要”而提前建设账号、数据库、付费或生图网关。后续增加功能时保留现有 slug、来源、原文和模板版本，避免破坏已经分享出去的链接。
