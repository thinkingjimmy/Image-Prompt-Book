/**
 * [INPUT]: 无
 * [OUTPUT]: 默认导出 null
 * [POS]: app/[locale]/(site)/@modal 的空分支（@modal/default.tsx），保证未拦截或离开详情时不残留旧弹窗
 * [PROTOCOL]: Update this header when making changes, then check README.md.
 */
export default function Empty() {
  return null;
}
